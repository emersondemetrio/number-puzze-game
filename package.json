{
  "scripts": {
    "watch": "tsc index.ts --outfile index.js --watch",
    "live": "live-server"
  },
  "devDependencies": {
    "install": "^0.13.0",
    "live-server": "^1.2.2",
    "typescript": "^4.8.3"
  }
}
